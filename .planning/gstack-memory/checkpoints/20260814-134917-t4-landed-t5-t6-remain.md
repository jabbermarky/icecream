---
status: in-progress
branch: claude/batch-loop-design
timestamp: 2026-08-14T13:49:17Z
files_modified: []
---

## Working on: t4 landed, t5-t6 remain

### Summary

P0.3 is code-complete through T4. `js/storage/recipe-sync-executor.js` is the
collect→plan→execute layer (strict listings + body downloads, abort on
listing failure with zero writes, any write failure skips ALL deletes,
`executeGatedPush` = pushRecipe's fetch→decide→write path) and
sync-manager.js is now thin wiring over it. T4 got the ONE full review
fan-out this PR is allowed under the token-discipline rules (specialists +
adversarial + codex): seven findings applied — Drive listing pagination,
Drive query-value escaping (apostrophe names duplicated files every save),
anchored filename decode (closes banked item 23 for listings),
listRecipes→Strict DRY inversion in both backends, IndexedDB strict listing
off the lossy updatedAt index, delete failures fold into sync status,
warning batching. Unit lane 170, browser green, everything pushed through
`79dcf87` (one mirror commit `a126c53` may need a push — stop hook will
say). **Remaining: T5, T6, then the #11 merge boundary.**

### Decisions Made

- **Token discipline is now CLAUDE.md law** (five rules, measured 5M+/3-day
  precedent): one review round per step with banked findings; full fan-out
  once per PR (T4 consumed this PR's); N=1 triage gate at design time
  (decision 14 precedent); NO polling automations — check-in triggers
  deleted, `subscribe_pr_activity` + maintainer's word are the only PR
  signals (maintainer: "I will not do anything on the PR without telling
  you"); fresh session per work block; batch small operations.
- Decision 14 (legacy conflicts REJECT) and amended decision 6 (mint only
  when keeping would duplicate) stand — see decision log.
- T4 review verdict: nine design-level findings BANKED (not fixed) as items
  9–18 of `.planning/todos/pending/2026-08-13-t2.6-round3-review-findings.md`,
  per rule 1. Sharpest: item 9 — sync's own rename residue (skipped/failed
  delete) re-joins as DUPLICATE_ID and stalls until hand-fixed. All 18
  banked items are merge-boundary review input.

### Remaining Work

1. **T5 — browser round-trip of a container built by buildRecipeContainer**
   (closes durability item 22; test-app.js). Contained step: built-in
   /code-review low/medium at most, no subagents.
2. **T6 — paperwork** (no review needed): lift DO-NOT-START in
   batch-loop-design.md; rollout note (reload every device, verify
   cache-busting, run scripts/migrate-legacy-recipes.js, deleted-record
   resurrection sentence — banked item 14); consolidated PR #11 body update
   covering T2.5→T4 (owed since T2; do it once here, not per-event).
3. **Merge boundary for #11:** consider the 18 banked items (fix the worthy,
   re-bank the rest as issues), then merge, close #11, fresh branch for
   P0.4+. NOTE: the full fan-out was ALREADY SPENT on T4 — the merge check
   is /code-review medium over the whole diff, not another army.
4. The binder read (maintainer) — gates P0.4/P0.7.
5. Background: ingredient issues #6–#10, durability items 18–21, #12, #13.

### Notes

- PR #11: open/draft, zero comments ever, no CI, mergeable. NO check-in
  triggers exist (all deleted, maintainer decision) — do NOT re-arm them.
  Re-arm ONLY subscribe_pr_activity on a fresh session.
- Session hygiene: this session is 3 days old — per rule 4, next work block
  should start a FRESH session and /context-restore this checkpoint.
- Codex works from this container (multiple clean runs Aug 13–14).
- Unit-lane gotchas: async .ier import needs a setTimeout-0 tick; stub-DOM
  render throws are console-only by design; the browser suite alone sees
  slider oninput/DisplayRecipe interactions (clear-after-display is
  load-bearing).
- scripts/migrate-legacy-recipes.js is throwaway, console-pasteable, not
  wired into the app; referenced by the T6 rollout note.
