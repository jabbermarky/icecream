# Design: Closing the Ingredient Onboarding Gap

Generated by /office-hours on 2026-08-11
Branch: claude/garry-tan-gstack-install-lp58z2
Repo: jabbermarky/icecream
Status: a record of a decision, not a plan. Current work: the issues listed under "The findings, and where each one is tracked" below.
Mode: Startup

## Problem Statement

Time-to-first-churn on a *new* recipe is measured in days. That time is spent on
research, not on formulation: identifying what goes into the flavor, reading
other recipes online to understand ratios, and then determining nutritional
values for ingredients not already in the library.

Ice Ed addresses only the last step of that sequence. Balancing to targets — PAC,
POD, fat, MSNF — is solved and is the app's core competence. Everything upstream
of balancing happens in browser tabs and on paper.

Stated plainly: **Ice Ed solves the last twenty minutes of a multi-day process
and none of the days in front of it.**

A second gap was named — improving on the previous batch of a recipe — but it
has a working workaround (see Status Quo) and costs convenience rather than days.
It is scoped out of this design and revisited under Dependencies.

## Demand Evidence

- **"I already keep notes on every batch that I make, and have been for > 1
  year."** Sustained manual behavior over a year, unprompted, with no tooling
  support. This is the strongest signal in the session: a workaround someone has
  maintained by hand for twelve months is a workaround that is load-bearing.
- **"Historically, time-to-first-churn took me days"** — the only quantified cost
  discussed. Nothing else in the session has a number attached to it.
- **The gap was self-identified seven months before this session.**
  `.planning/todos/pending/2026-01-14-versioned-recipes-in-library.md` was filed
  2026-01-14 and is still pending. Three related AI todos were filed 2026-01-15.
  The problem has been legible to the founder since January; the effort has gone
  elsewhere (the Sprinkles rewrite) in the interim.
- **Existing partial investment.** The USDA FoodData Central integration in
  `js/features/ingredients.js` was built to attack exactly this problem, and it
  goes deep — it derives PAC and POD from the individual sugar breakdown. Someone
  cared enough to write real domain math against it.

**Honest limitation: N=1.** All demand evidence here comes from a single user who
is also the developer. There is no evidence in this session that other people
experience this pain, and the Sprinkles decisions doc assumes a multi-user
product without any such evidence in hand. That gap is not resolved by this
design and should not be papered over: this is a design for a tool that is
known-valuable to exactly one person.

## Status Quo

The current workflow for a new recipe:

1. Flavor profile research — read multiple ice cream recipes on cooking sites,
   subreddits and blogs; reconcile them into a rough ingredient list and ratios.
   Entirely outside the app.
2. Ingredient research — for anything not already in the library, find
   nutritional values. The app's USDA import handles part of this
   (`ingredients.js:635`), but see Premises P2: it routinely declines to
   populate PAC and POD, the two values formulation actually needs, and those get
   computed by hand in the PAC/POD calculator tool.
3. Balance in Ice Ed. This part works.
4. Churn, then record the outcome in handwritten batch notes kept outside the
   app entirely.

For iterating on an existing recipe: consult the handwritten notes, adjust the
formulation manually, churn again. The notes and the recipe are never linked;
correlation happens in the founder's head.

## Target User & Narrowest Wedge

**Target user:** an experienced formulator maintaining a curated personal
ingredient library, who already understands PAC/POD/MSNF and does not need the
concepts explained — someone for whom the bottleneck is data acquisition, not
knowledge. Today that is a population of one.

**Narrowest wedge:** *not* "build ingredient onboarding." A code read during this
session established that ingredient onboarding largely exists already. The wedge
is closing three specific gaps in the onboarding that ships today (G1, G2, B1
below). This is materially smaller than what was proposed at the start of Phase
4, and the reduction is the main product of the session.

**What grew, and why.** The task list is ten items, not three. The wedge held;
these attached to it, each accepted deliberately at the time:

| Item | Relationship to the wedge |
| --- | --- |
| vendor `idb` | **Prerequisite.** The test suite could not run at all until this landed (10 passed / 15 failed → 113 / 1). Nothing in the wedge was verifiable without it. |
| FDC error handling (#57) | **Adjacent, same function.** Every non-200 response failed silently. A feature whose value is "saves an evening" must not fail in a way indistinguishable from "no data exists." |
| Foundation preference (#59) | **Adjacent, found by measurement.** The code prefers the record type that carries no sugar data, so it selects the record that cannot produce PAC/POD. |
| optimizer logging (#60) | **Opportunistic, unrelated.** Four `console.log` per ingredient per `Sums` read, inside the optimizer's objective function. Free win, no connection to ingredients. |

The node test lane (#7) and the regression guards (#8) are test infrastructure
for the above rather than separate scope.
**Use this table as the boundary test for any eleventh task:** if it is neither
inside G1/G2/B1 nor a prerequisite for verifying them, it needs the same
explicit acceptance these did.

What already works in `js/features/ingredients.js:635` (`onDownloadIngredientData`):
searches FDC across Foundation / Survey (FNDDS) / SR Legacy; retries with
truncated queries on zero hits; fuzzy-matches by Damerau-Levenshtein and presents
a pick-list; prefers Foundation over SR Legacy over Survey; derives Water, Fat,
Sugar, kcal; derives PAC and POD from the sugar breakdown against the `Sugars`
reference table; derives Solids and MSNF; reports unpopulated fields; routes
through `importIngredients` for diff-and-confirm rather than silent overwrite.

## Constraints

- **Solo maintainer.** Anything requiring ongoing operational attention is
  expensive out of proportion to its size.
- **The app is in real use.** The existing curated `data/ingredients.json` is
  years of accumulated work and must not be disturbed. Any schema change must be
  additive and backward-compatible.
- **No backend today.** The current app is static; the USDA call goes directly
  from the browser. Anything requiring a server-side proxy (e.g. an LLM key)
  crosses a line the current architecture does not cross.
- **Vanilla ES6 modules, Playwright tests.** Work lands in the existing app, not
  in the Sprinkles rewrite.
- **An in-flight rewrite plan exists** (`.planning/designs/sprinkles-stack-decisions.md`)
  that would replace this codebase. Work here must be justifiable even if the
  rewrite proceeds — see Dependencies.

## Premises

- **P1 — The days are dominated by research, not balancing.** *Confidence: high.*
  Stated directly by the founder and consistent with the app's feature coverage.
- **P2 — Manual PAC/POD entry is a major share of ingredient research time.**
  *Confidence: high. **VERIFIED 2026-08-11** against the live FDC API.* The
  derivation gate passes for **0 of 11** representative ice cream ingredients
  under the current code. It has never fired. The cause is **not** what this
  document originally claimed — see "Correction" below.
- **P3 — FDC covers most ingredients actually being added.** *Confidence:
  medium-high. **Partially verified 2026-08-11.*** SR Legacy records carry rich
  sugar data: 10 of 11 report `Total Sugars`, and 8 of 11 carry an individual
  breakdown that reconciles against it. Foundation records carry none at all.
  Coverage is better than feared, which weakens the case for the G3 LLM
  fallback. Still unmeasured: how many ingredients actually being added are
  absent from FDC entirely.
- **P4 — A clearly labelled estimated value is more useful than a blank field.**
  *Confidence: medium.* The opposite is defensible: a blank forces attention, an
  estimate can be ignored. Mitigated by requiring estimates to be visually
  distinct at point of *use*, not only at import.
- **P5 — Review-before-commit is a feature, not friction.** *Confidence: high.*
  A wrong fat percentage entering the library unchallenged corrupts formulations
  silently for months.

## Approaches Considered

### Approach A: Ingredient onboarding — close the gaps in what exists
Fix the three defects in the existing USDA import: graded PAC/POD derivation
(G1), per-field provenance (G2), and the fuzzy-match sort bug (B1).
*Completeness 7/10. ~1 week human / ~half a day CC.*
Attacks the blocker present inside every new recipe. Mechanical enough that no
AI judgment is involved and nothing can be silently wrong in a way review won't
catch. Builds on code that already exists and already encodes the domain math.
Does not touch flavor research, so part of the multi-day cost survives.

### Approach B: Full recipe importer from web sources
Build `.planning/todos/pending/2026-01-15-ai-recipe-importer-from-web.md`: paste
a URL or raw text, LLM parses it, converts volume to weight, fuzzy-matches the
library, researches missing ingredients, shows per-field confidence, user
approves into a draft.
*Completeness 9/10 on the gap, high variance. ~3 weeks human / ~1-2 days CC.*
The single biggest possible cut to time-to-first-churn. But it strictly subsumes
Approach A — if the ingredient half is unreliable the whole feature is
untrustworthy — and its hardest problem is calibrating when to say "I am not
sure." Requires an LLM key, therefore a backend, therefore crosses the
no-server constraint.

### Approach C: Flavor research chat
An LLM chat grounded in the ingredient library and current targets: "what goes
into a Sicilian pistachio gelato and at what ratios."
*Completeness 6/10. ~2 weeks human / ~1 day CC.*
Hits the half of the gap the founder named *first*, and the half that is slow
because it requires judgment rather than lookup. But it is the least verifiable
output in the whole space — plausible nonsense is indistinguishable from good
advice until after a churn — and it also requires the backend.

### Approach D: Close the other gap instead — link a year of batch notes to recipe versions
Import the existing handwritten notes, anchor each to the recipe version that
produced it.
*Completeness 8/10 on gap 1. ~1 week human / ~half a day CC.*
There is over a year of real data sitting outside the app, and it is the corpus
any future "improve this recipe" feature would need. But gap 1 already has a
workaround that has held for a year — this buys convenience, not days — and it
leaves the expensive gap untouched.

## Recommended Approach

**Approach A**, with scope revised downward from what was originally proposed.

Rationale:

1. **It targets the one place a small fix plausibly removes hours.** G1 is a
   five-line defect sitting between the user and the two numbers the app exists
   to compute.
2. **It requires no backend**, so it does not force the API-proxy decision that
   B and C both drag in.
3. **It is a strict prerequisite for B.** The importer's hardest sub-problem is
   trustworthy ingredient values with confidence semantics. Building A first
   gives B somewhere honest to put its output.
4. **It forces `createIngredient` to exist as a domain concept**, with
   provenance, before `recipe-domain` (D13) is extracted — see Dependencies.
5. **A and D are close in cost, and A addresses the gap with a number attached.**

### Scope

**In:**
> **Where the buildable detail lives.** This section states *why* each item is
> in scope. **Implementation Tasks is authoritative for what to build** — file
> paths, exact rules, verification. When a decision changes, that section is the
> one to update. This split exists because an earlier revision left six
> passages describing designs that had been abandoned, which is what three
> copies of a decision reliably produces.

- **G1 — make the derivation fire.** The PAC/POD derivation has never once run
  in production: measured, it fails for **0 of 11** representative ingredients.
  The cause is nutrient-name drift, not chemistry — FDC renamed fields the code
  still asks for, and the lookups fail silently. A side effect nobody had
  noticed: total `Sugar` was never populating either.

  Two parts. **G1a** corrects the names, which alone takes the gate from 0/11 to
  6/11 — the highest value-per-line change in the plan. **G1b** handles records
  with a partial sugar breakdown by reconciling what is present against the
  reported total, rather than consulting a hardcoded table of which sugars are
  safely zero.

  Reconciliation was chosen over the hardcoded list proposed during review
  because measurement showed it is both simpler and more accurate: honey
  reconciles to within 0.03 g while reporting no lactose, because honey contains
  none. A hardcoded list would have marked that estimated. The data validates
  itself, and no chemistry table needs maintaining.

  **Measured outcome across 11 representative ingredients:** 8 measured,
  2 estimated, 1 undeliverable — against 0 today. See T2a and T2b.
- **G2 — per-field provenance.** Record source (`usda:foundation`,
  `usda:sr-legacy`, `derived`, `estimated`, `manual`, later `llm`), FDC ID where
  applicable, and date. Additive sidecar; unannotated entries read as `manual`.
  Not "done" until provenance is visible in the ingredient editor.
- **B1 — sort comparator (#58).** `distances.sort()` (`:673`) sorts lexicographically,
  so the `minDistance` threshold (`:674`) is miscalibrated and the filter
  (`:675-678`) keeps and discards the wrong candidates. Fix:
  `.sort((a, b) => a - b)`. Independently testable; worth doing regardless.

**Out, deliberately:** G3 (LLM fallback for ingredients FDC lacks) — depends on
G1/G2 and pulls in the backend question. Approach B in full. Flavor-profile
research. Anything in the Sprinkles rewrite.

**Non-goal:** full automation. See P5.

**Flagged, not in scope:** `js/features/ingredients.js:650` carries a hardcoded
USDA API key in a client-side request. It is in version control *and* exposed to
every user of the app regardless. Low severity — FDC keys are free and
rate-limited, not billable — but it should be rotated and moved to config.

## Open Questions

1. **Roughly what fraction of the ingredients added in the past year did FDC
   actually have?** *Partially answered 2026-08-11:* for common ice cream
   ingredients FDC coverage is good and SR Legacy sugar data is rich. Still open
   is the specialty end — pistachio paste, glucose syrups by DE, commercial
   stabilizer blends — which is where G3 would earn its place.
2. **When a record has `Total Sugars` but no breakdown at all, what does the
   apportioned remainder get attributed to?** *Narrowed 2026-08-11:* this is now
   known to affect 2 of 11 reference ingredients (cocoa powder, vanilla
   extract), and the reconciliation rule means it no longer fires for records
   that merely omit a sugar which is genuinely zero. Sucrose is the obvious
   default and is wrong for some foods; declining and prompting for manual entry
   is the alternative.
3. **Should re-importing an existing ingredient auto-upgrade estimated values to
   measured, or always require review?**

## Success Criteria

- **Primary — two numbers, deliberately not one.** The original single ">80%
  populated" criterion could not be evaluated once estimates existed: the same
  implementation scores 91% counting them and 73% excluding them. Splitting the
  metric keeps how often the app is *guessing* visible instead of absorbed into
  a headline figure that can be passed by guessing more.

  | Criterion | Baseline (measured 2026-08-11) | Target |
  | --- | --- | --- |
  | Populates PAC/POD as **measured** (breakdown reconciles) | 0/11 → **8/11 (73%)** after T2a+T2b | >70% |
  | Populates PAC/POD **at all** (measured or estimated) | 0/11 → **10/11 (91%)** | >85% |
  | *Estimate rate* (the gap between them) | **2/11 (18%)** | watch, not a target |

  A rising estimate rate is a signal about FDC data quality, not a regression to
  fix by itself. Baseline is measured, not aspirational: the pre-fix value was
  0 on both criteria.
- Every PAC/POD value carries a visible measured-vs-estimated marker at point of
  use in a formulation, not only at import.
- Existing `data/ingredients.json` loads unchanged with no migration step.
- The fuzzy-match fix is covered by a test that fails against the current
  lexicographic sort.
- **Qualitative:** on the next new recipe, ingredient research is no longer the
  step that dominates elapsed time.

## Distribution Plan

Static web app; the change ships through whatever path already publishes Ice Ed.
No new distribution surface, no new build step, no new runtime dependency — G1
and B1 are logic changes and G2 is an additive data field. Playwright tests run
via `npm test` as today.

## Dependencies

- **No blockers.** All three items are self-contained within
  `js/features/ingredients.js` plus the ingredient data shape.
- **Relationship to the Sprinkles rewrite** — two findings that hold regardless
  of whether the rewrite proceeds:
  - **D9's command vocabulary cannot express ingredient creation.**
    `setIngredientAmount(id, grams)`, `addIngredient(id, grams)`,
    `removeIngredient(id)`, `setTargetPAC`, `setTargetFat`, `optimizeRecipe`
    (`sprinkles-stack-decisions.md:104-109`) every one takes an ID that already
    exists. There is no `createIngredient`. The vocabulary fits recipe *editing*
    — which is what D12 was designed around, and which fits the two chat todos
    D12 absorbs (`:232`). It does not fit "this ingredient does not exist yet."
  - **`ai-recipe-importer-from-web.md` is not carried into the decisions doc.**
    Of the three January AI todos, D12 names the research and troubleshooting
    ones. The importer — unit conversion, library matching, USDA research for
    unknowns, per-field confidence — is neither adopted nor listed under
    Rejected Alternatives. It appears to have fallen out because it did not fit
    the editing-centric frame, not because it was judged low-value.
- **Deferred, not dropped:** Approach D (linking batch notes to recipe versions)
  and `2026-01-14-versioned-recipes-in-library.md` remain the natural follow-on.

## The Assignment

*Superseded in part on 2026-08-11.* The original assignment was to audit the
last 20 ingredients for two things: whether FDC had them, and whether PAC/POD
were typed in by hand. **The second half is now answered by direct
measurement** — the gate passes 0 of 11, so PAC and POD were always typed by
hand. No audit needed.

**What remains worth an hour: list the last 20 ingredients you added and mark
which ones FDC did not have at all.** Not whether the import worked — it never
did — but whether the ingredient existed in the database. Common dairy, sugars,
fruit and nuts are well covered. The open question is the specialty end:
pistachio paste, glucose syrup by DE, commercial stabilizer blends, regional
products.

That count is the only thing that decides whether G3, the LLM fallback this
design scopes out, is a nice-to-have or the actual next priority after G1.

## What I noticed about how you think

- **You answer product questions with evidence instead of opinions.** Asked what
  the gap was, you said *"I already keep notes on every batch that I make, and
  have been for > 1 year."* Most founders answer that question with a theory.
  A year-long unbroken manual habit is the kind of proof that is very hard to
  fake and very easy to overlook in your own behavior.
- **You rejected the frame rather than picking from it.** Offered four options,
  you took none and instead decomposed the problem your own way: *"There are
  multiple workflow gaps that I want to work on: (1)... (2)..."* The reframing
  was better than the menu, and it is what made the session useful.
- **You push back on compression.** *"I don't understand your last comment"* —
  and the claim in question turned out to be genuinely overstated when I went and
  checked it. You did not nod along to a confident-sounding sentence, and the
  correction improved the analysis.
- **You track process while deep in content.** *"We finished the office hours
  session and jumped immediately into coding. Doesn't seem like we are following
  the workflow."* You were several layers into ingredient chemistry and still
  noticed the method had gone off the rails. That is unusual, and it caught a
  real failure — the design doc had been written to the wrong place, in the
  wrong format, skipping the review handoff entirely.

---

# Engineering Review — 2026-08-11

Seven issues raised, seven resolved. Scope accepted as-is (complexity check did
not trigger: ~4 files, no new services). The review changed the design in three
material ways, recorded below.

## What already exists

| Sub-problem | Status | Plan's treatment |
| --- | --- | --- |
| FDC search, truncate-retry, pick-list | Exists — `ingredients.js:635-707` | Reused |
| Foundation > SR Legacy > Survey preference | Exists — `:714-735` | Reused |
| PAC/POD derivation from sugar breakdown | Exists **twice** — `ingredients.js:776-786` and `tools.js:174-179` | **Was going to be rebuilt in one copy only. Now extracted and shared (Issue 2).** |
| Diff-and-confirm before overwrite | Exists — `importIngredients:515` | Reused |
| Damerau-Levenshtein distance | Exists — `helpers.js` | Reused; ranking around it rewritten (Issue 5) |
| Playwright harness, 24 methods / 116 assertions | Exists — `test-app.js` | Extended; new node lane added alongside (Issue 6) |
| Value-asserting tests for PAC/POD math | **Does not exist** | Added (Issue 6) |

The central correction: this plan originally read as "build ingredient
onboarding." Onboarding already exists and is substantial. The work is closing
defects in it.

## Review-driven changes to the design

1. **G1 is two nutrient-name constants, plus a reconciliation rule** (Issue 1,
   **corrected 2026-08-11 — see Correction below**). The original review finding
   blamed galactose. Live API data disproved it.
2. **A DRY extraction now precedes G1** (Issue 2). The derivation is duplicated
   across the importer and the PAC/POD calculator at 100× different unit scales.
   Changing one copy would have silently desynchronized the user's own
   cross-check tool. Structural commit first, behavioral second.
3. **Two defects were found that the design doc never mentioned** — silent
   failure on every non-200 FDC response (Issue 4), and debug logging inside the
   optimizer's hot path (Issue 7).

## Correction — measured against the live API, 2026-08-11

> **Frozen record. Do not edit this section.** It documents what was believed
> and how it was disproven, dated to when that happened. Its value is being an
> unaltered account. If a later finding changes the design again, add a new
> dated section rather than revising this one, and update Implementation Tasks.

Network egress to `api.nal.usda.gov` was opened after the review completed. The
premise the review rested on could then be measured instead of inferred. **The
review's diagnosis was wrong on the mechanism, right on the consequence.**

**What the review claimed:** the gate fails because it requires galactose, which
USDA rarely reports.

**What the data shows:** galactose is present in 6 of 7 records first probed and
in every SR Legacy record with a sugar profile. The gate fails because of two
nutrient-name mismatches — the code asks FDC for `"Glucose (dextrose)"` and
`"Sugars, Total NLEA"`, and FDC returns `"Glucose"` and `"Total Sugars"`. Both
lookups return `-1.0` and fail silently.

**Consequence, unchanged and now proven:** the gate passes for **0 of 11**
representative ingredients under current code. The derivation has never fired.
Also newly discovered: because the total-sugars name never matches either,
`Sugar` has not been populating on import at all — a field nobody had noticed
was broken.

**A third finding the review missed entirely.** Foundation records carry no
sugar breakdown and no `Total Sugars`:

```
Cheese, ricotta, whole milk [Foundation]
   OK       Water, Total lipid (fat), Energy
   MISSING  Sucrose, Glucose, Fructose, Lactose, Maltose, Galactose, Total Sugars
```

The code prefers Foundation over SR Legacy over Survey (`:714-735`), so for
formulation purposes it deliberately selects the record that **cannot** produce
PAC/POD over the one that can. Foundation has better sampling metadata; SR
Legacy has the sugar chemistry. The preference should depend on whether the
record actually carries sugar data.

**Measured gate results, 11 representative ice cream ingredients:**

| | current code | + corrected names | + reconciliation rule |
| --- | --- | --- | --- |
| Derives, marked measured | 0 | 6 | **8** |
| Derives, marked estimated | 0 | 0 | 2 |
| Cannot derive | 11 | 5 | 1 |

The single undeliverable case is nonfat dry milk, which reports no individual
sugars and no total. It should decline and say so.

**What this changes about the plan:** the derivation fix shrinks from a
chemistry rule to two string constants plus a reconciliation check. The hardcoded safely-zero sugar
list agreed during review is dropped as unnecessary. A new task (#59) covers the
Foundation preference. P2 moves from unverified to verified; P3 improves.

## NOT in scope

| Deferred | Rationale |
| --- | --- |
| G3 — LLM fallback for ingredients FDC lacks | Depends on G1/G2 landing first; requires an API key, therefore a backend, which this app deliberately does not have |
| Full web recipe importer | `.planning/todos/pending/2026-01-15-ai-recipe-importer-from-web.md`; strictly larger, and depends on trustworthy ingredient values existing first |
| Flavor-profile research | The other half of the multi-day gap; least verifiable output, also needs a backend |
| Approach D — batch notes linked to recipe versions | Real, but gap 1 has a year-old working workaround; see `2026-01-14-versioned-recipes-in-library.md` |
| Fixing absent-vs-zero in `cIngredient` | Issue 3 chose the additive sidecar; changing the constructor would produce diff noise against years of curated data |
| USDA API key rotation | Captured as `.planning/todos/pending/2026-08-11-rotate-usda-api-key.md` |
| Anything in the Sprinkles rewrite | This lands in the current vanilla app |

## Failure modes

| New codepath | Realistic production failure | Test? | Error handling? | Silent? |
| --- | --- | --- | --- | --- |
| `deriveSugarProperties` — reconciliation | Breakdown reconciles within tolerance but a sugar is genuinely mis-reported by FDC, yielding a plausible but wrong PAC marked *measured* | node-lane table cases (#7) | Value marked measured/estimated | **Yes — chemistry error, not a crash** |
| Alias fallback (`firstNutritionValue`) | Loop nesting inverted, so a lower-priority record's primary name beats a higher-priority record's alias and one profile mixes two USDA records | node-lane nesting case (#7) | None — silent | **Yes.** Occurred twice in implementation; caught by cross-model review, not by tests |
| `deriveSugarProperties` — unit scale | Caller passes percent where fraction expected; every value off by 100× | node lane + regression guards, cross-surface | Explicit unit parameter | Partially — 100× is visible if you look |
| `deriveSugarProperties` — empty breakdown | Total Sugar > 0, no sub-sugars at all. **Behavior undecided** | Not yet | Not yet | **Yes — critical gap** |
| `rankCandidates` | Correct match ranked out of the pick-list; user concludes FDC lacks the ingredient | node lane (#7) | None possible — ranking is heuristic | **Yes** |
| `resultHandler` error branches | 429 mid-session; button appears dead | regression guards (#8) | Added by Issue 4 | No, after Issue 4 |
| Provenance sidecar | Legacy entry with no sidecar throws on read | node lane + regression guards | Defaults to `manual` | No |

**Critical gaps: 1.** The empty-breakdown case has no test, no handling, and
would fail silently by producing no PAC/POD with no explanation. It must be
decided before G1 is implemented.

## The findings, and where each one is tracked

**This section used to be a task checklist with time estimates and a build
order.** It is neither now: order lives in the issues, state lives in the
issues, and the estimates were guesses that were being read as measurements.
What remains is what each finding *is* and what would settle it.

The `T0–T9` numbering is retired. It collided outright — `T1`, `T5` and `T6`
each named two unrelated tasks, one series here and another inside the recipe
identity work (#44) — which is a large part of why #56 exists.

| Finding | Where it lives |
|---|---|
| The sugar→PAC/POD formula exists twice, 100× apart in scale | **#6** |
| The breakdown is not reconciled against `Total Sugars` | **#9** |
| Measured-zero is unrepresentable, so provenance needs a sidecar | **#10** |
| No node unit lane with table-driven derivation cases | **#7** |
| The two critical regression guards are missing | **#8** |
| Non-200 FDC responses fail silently | **#57** |
| Match ranking sorts distances lexicographically | **#58** |
| The Foundation preference selects the record with no sugar data | **#59** |
| Debug logging in the optimizer hot path | **#60** |

**Landed, and the reason is worth keeping:**

- **`idb` is vendored** rather than imported from `esm.sh` inside the startup
  module graph. The suite went from 10 passed / 15 failed to 113 / 1, and the
  runtime from 358s to 49s. Allowing `esm.sh` through the proxy was **not**
  sufficient — Playwright's Chromium does not inherit `HTTPS_PROXY` and still
  got `ERR_CONNECTION_RESET`. Vendoring was the actual fix, and it removes a
  third-party CDN as a production single point of failure.
- **The two nutrient name constants are fixed.** FDC returns `"Glucose"`, not
  `"Glucose (dextrose)"`, and `"Total Sugars"`, not `"Sugars, Total NLEA"`.
  `getNutritionValue` returns `-1.0` on no match and the validity check ANDs
  across all seven sugar keys, so **one** unmatched name suppressed both PAC
  and POD; the total-sugars mismatch meant `Sugar` never populated either,
  since `setValue` skips negatives. Measured against the live API: **PAC/POD
  gate 0/11 → 6/11**, **`Sugar` populates 0/11 → 10/11**.

  Old names are retained as fallbacks via `firstNutritionValue()`, an ordered
  first-match helper. **Not `Math.max`** — an earlier attempt used it, and
  taking the max across aliases discards FDC record priority, letting a
  lower-priority record's value win and assembling one ingredient's profile
  from two different USDA records. The helper iterates records outer, aliases
  inner. **The other nesting looks equivalent and reintroduces the defect**;
  it occurred twice during implementation and was caught by review, not by
  tests.

**One dependency that is real rather than preference:** `firstNutritionValue()`
is a closure inside `onDownloadIngredientData` and is not exported, so it
cannot be unit tested until #6 extracts it. #6 gates the derivation half of #7.

**One thing is still undecided**, and #9 is blocked on it: where the
unattributed shortfall goes when the individual sugars sum to less than
`Total Sugars`. See the open decision above.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 4 | clean | 5 → 3 → 3 → 0 across four passes |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 2 | clean | Pass 1: 7 issues. Pass 2 (doc): 4 issues, all resolved |
| Diff Review | `/review` | Pre-landing diff check | 1 | clean | 3 informational, 2 fixed |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

**CORRECTION (2026-08-11, post-review):** Issue 1's diagnosis was wrong on the
mechanism. Measured against the live API after network egress was opened, the
gate fails on two nutrient-name mismatches, not on galactose coverage. The
consequence is unchanged and now proven at 0/11. See "Correction" above. The
derivation fix split in two — the name constants (landed) and reconciliation
(#9) — the hardcoded safely-zero sugar list was dropped, and the Foundation
preference (#59) was added.

**CROSS-MODEL:** Codex ran four adversarial passes over the branch, converging
5 → 3 → 3 → 0 findings. Two pass-2 findings were defects the pass-1 fixes had
introduced, and the pass-3 P1 was the third distinct variant of a bug fixed
twice. Pass 4 returned clean at high confidence, having reproduced the edge
cases rather than reasoning about them. The self-review that preceded it found
3 informational issues on the same diff; Codex found 8, five of them P1. Where
they diverged, Codex was right every time.

**VERDICT:** ENG REVIEW COMPLETE (2 passes) + DIFF REVIEW + CROSS-MODEL CLEARED.
Pass 1: 7 issues, all resolved, 1 corrected by later measurement. Pass 2 (this
doc): 4 issues, all resolved. 10 implementation tasks, 2 shipped and verified
against the live API. Suite 114 passed / 0 failed. Not fully CLEARED: one
decision remains open below, gating T2b only — every other task is unblocked.

**UNRESOLVED DECISIONS:**
- When a record carries `Total Sugars` but no individual breakdown at all
  (measured: 2 of 11 — cocoa powder, vanilla extract), what does the apportioned
  remainder get attributed to? Sucrose is the obvious default and is wrong for
  some foods. The alternative is to decline and prompt for manual entry. This is
  narrower than the original open question — the no-total case (1 of 11) is
  settled: decline and state why — but it still gates T2b.
