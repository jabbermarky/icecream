---
phase: 14-save-workflow
plan: 01
type: execute
depends_on: []
files_modified: [js/features/recipe-manager.js, index.html, js/app.js, test-app.js]
---

<objective>
Integrate save workflow to save recipes to the local library instead of downloading files.

Purpose: Transform "Save" from file download to library storage, completing the recipe library feature. Keep file export as secondary option.
Output: Save button saves to IndexedDB library with feedback; "Export to file" available as separate action.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior phase summaries (dependency graph):**
@.planning/phases/12-storage-interface/12-01-SUMMARY.md
@.planning/phases/13-recipe-library-ui/13-01-SUMMARY.md
@.planning/phases/13-recipe-library-ui/13-02-SUMMARY.md

**Source files:**
@js/features/recipe-manager.js
@js/storage/storage.js
@js/storage/indexeddb-storage.js
@js/ui/recipe-library.js
@js/app.js
@index.html

**Key decisions from prior phases:**
- Phase 12: Storage interface pattern with `saveRecipe({ name, data })` contract
- Phase 12: Graceful error handling (return null/empty, don't throw)
- Phase 13: Callback pattern for library actions
- Phase 13: Browser confirm() for destructive actions

**Current save workflow (recipe-manager.js lines 1178-1203):**
- `handleSaveRecipe()` builds container `{ Recipe, Ingredients }`
- Calls `saveToFile()` to download
- Calls `SetRecipeModified(false)` after save

**Storage contract (indexeddb-storage.js):**
- `saveRecipe({ name, data })` - expects `{ name: string, data: { Recipe, Ingredients } }`
- `hasRecipe(name)` - returns Promise<boolean> for existence check
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify handleSaveRecipe to save to library</name>
  <files>js/features/recipe-manager.js</files>
  <action>
Modify `handleSaveRecipe()` function to save to IndexedDB library instead of downloading:

1. Add `storage` parameter to `initRecipeButtons({ ..., storage })` - this provides the storage instance
2. Store storage reference at module level (like other injected deps)
3. Modify `handleSaveRecipe()` to be async:
   - Build container as before `{ Recipe, Ingredients }`
   - Format for storage: `{ name: Recipe.Name, data: container }`
   - Check if exists with `await storage.hasRecipe(Recipe.Name)`
   - If exists, show confirm dialog: `confirm('Recipe "X" already exists. Overwrite?')`
   - If user confirms (or doesn't exist), call `await storage.saveRecipe(recipeData)`
   - Show success message using existing status functions (StatusMsg or similar)
   - Call `SetRecipeModified(false)` after successful save
4. Keep existing validation (empty name check, ingredient warnings)

Avoid direct DOM manipulation in handleSaveRecipe - use injected storage instance. Follow existing async patterns in the codebase.
  </action>
  <verify>Manual test: Create/modify recipe, click Save, verify recipe appears in Library modal with updated timestamp</verify>
  <done>handleSaveRecipe saves to IndexedDB, shows overwrite confirmation if exists, clears modified indicator on success</done>
</task>

<task type="auto">
  <name>Task 2: Add Export to File button and wire up storage</name>
  <files>index.html, js/app.js</files>
  <action>
1. In index.html, rename existing Save button and add Export button:
   - Change btnSaveRecipe text from "Save to file ..." to "Save" (shorter, cleaner)
   - Change title to "Save recipe to your library"
   - Add new button: `<button type="button" id="btnExportRecipe" title="Export recipe to a file for backup or sharing">Export to file ...</button>`
   - Place Export button after Save button (order: Save, Export, Load from file, Library)

2. In js/app.js, update initRecipeButtons call:
   - Add `storage: recipeStorage` to the buttons object passed to initRecipeButtons
   - Add btnExportRecipe to the buttons object

3. In js/features/recipe-manager.js:
   - Add `btnExportRecipe` to initRecipeButtons destructuring
   - Create `handleExportRecipe()` function with current saveToFile logic (moved from handleSaveRecipe)
   - Wire `btnExportRecipe.onclick = handleExportRecipe`

This separates concerns: Save = library (default), Export = file download (backup/sharing).
  </action>
  <verify>`npm test` passes, both Save and Export buttons visible in Recipe tab</verify>
  <done>Save button saves to library, Export button downloads file, both buttons functional</done>
</task>

<task type="auto">
  <name>Task 3: Add Playwright tests for save workflow</name>
  <files>test-app.js</files>
  <action>
Add test method `testSaveWorkflow()` to verify the new save behavior:

1. Test: Save new recipe to library
   - Create new recipe with name "Save Test Recipe"
   - Add an ingredient
   - Click Save button
   - Open Library modal
   - Verify recipe appears in list

2. Test: Save updates existing recipe
   - Load "Save Test Recipe" from library
   - Modify recipe (change name or add ingredient)
   - Click Save (should prompt overwrite)
   - Accept overwrite
   - Verify modified recipe saved (check timestamp updated)

3. Test: Export to file works
   - Setup download listener
   - Click Export button
   - Verify download triggered with .ier extension

4. Cleanup: Delete test recipe from storage

Add testSaveWorkflow() to the test runner list in runTests().
  </action>
  <verify>`npm test` passes with new save workflow tests</verify>
  <done>Save workflow tested: new save, overwrite, export to file all verified</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm test` passes all tests including new save workflow tests
- [ ] Save button saves recipe to IndexedDB (verify in Library modal)
- [ ] Overwrite confirmation appears when saving existing recipe name
- [ ] Export button downloads .ier file
- [ ] Modified indicator clears after successful save
- [ ] Recipe name validation still works (empty name shows warning)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Save workflow changed from file download to library storage
- File export still available as secondary action
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/14-save-workflow/14-01-SUMMARY.md`
</output>
