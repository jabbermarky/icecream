---
phase: 15-polish
plan: 01
type: execute
depends_on: []
files_modified: [js/storage/indexeddb-storage.js, js/features/recipe-manager.js, js/ui/recipe-library.js, js/app.js, css/styles.css]
---

<objective>
Polish the recipe library feature for daily use reliability.

Purpose: Make save/load/delete operations trustworthy with clear feedback, and ensure the library UI handles real collections (40-50+ recipes).
Output: User-facing error/success feedback for all storage operations; scrollable recipe list.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-polish/15-CONTEXT.md

# Prior phase context
@.planning/phases/14-save-workflow/14-01-SUMMARY.md

# Key source files
@js/storage/indexeddb-storage.js
@js/ui/recipe-library.js
@js/features/recipe-manager.js
@js/ui/components.js
@css/styles.css

**Established patterns:**
- `Info(message)` for success feedback (lightbulb icon, 3s timeout)
- `Warning(message)` for warnings (warning icon, 6s timeout)
- `ErrorMsg(message)` for errors (stop icon, 10s timeout, red background)
- Storage returns null/empty on failure (graceful degradation)
- Browser `confirm()` for destructive actions

**Constraining decisions:**
- Phase 12: Graceful error handling (return null/empty, don't throw)
- Phase 14: Use existing Info() function for success messages
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add success/error feedback for storage operations</name>
  <files>js/storage/indexeddb-storage.js, js/features/recipe-manager.js, js/ui/recipe-library.js, js/app.js</files>
  <action>
1. Modify IndexedDBStorage methods to return boolean success indicators:
   - `saveRecipe()`: return true on success, false on error (currently returns undefined)
   - `deleteRecipe()`: return true on success, false on error (currently returns undefined)
   - Keep loadRecipe/listRecipes/hasRecipe as-is (they already return meaningful values)

2. Update handleSaveRecipe() in recipe-manager.js:
   - Check saveRecipe() return value
   - On failure: show `ErrorMsg('Failed to save recipe. Please try again.')`
   - Keep existing Info() on success

3. Update onDelete callback in app.js:
   - Check deleteRecipe() return value
   - On success: keep existing Info()
   - On failure: show `ErrorMsg('Failed to delete recipe.')`

4. Update onLoad callback in app.js:
   - Load feedback already exists (Info on success, Warning on null)
   - No changes needed

Import ErrorMsg from components.js where needed.
  </action>
  <verify>
- Save a recipe, verify Info message appears
- In browser console, manually break storage (e.g., `window.indexedDB.deleteDatabase('ice-ed-recipes')`) and try save - verify ErrorMsg appears
  </verify>
  <done>
- saveRecipe/deleteRecipe return boolean
- ErrorMsg shown on storage failures
- Info shown on success (existing behavior preserved)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add scrollable recipe list for large libraries</name>
  <files>css/styles.css</files>
  <action>
Add CSS to make recipe library list scrollable when it exceeds viewport:

1. Add max-height to .recipe-library-list tbody (e.g., 50vh or calc-based)
2. Add overflow-y: auto to enable scrolling
3. Ensure table header stays visible (sticky positioning)

Keep it simple - just needs to handle 40-50 recipes without breaking. The modal already has overflow:auto, but the table needs internal scrolling to keep headers visible.
  </action>
  <verify>
Add test recipes via console:
```js
const storage = window.recipeStorage;
for (let i = 1; i <= 30; i++) {
  await storage.saveRecipe({ name: `Test Recipe ${i}`, data: { Recipe: { Name: `Test Recipe ${i}` }, Ingredients: {} } });
}
```
Open library modal - list should scroll while header stays visible.
  </verify>
  <done>
- Recipe list scrolls when >15-20 recipes
- Table header remains visible during scroll
- No horizontal scrolling introduced
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Storage feedback and scrollable library list</what-built>
  <how-to-verify>
1. Run: Open index.html in browser (Live Server or python -m http.server)
2. Create and save a recipe:
   - New Recipe > Enter name > Add ingredient > Save
   - Verify: Info message appears in status bar
3. Open Recipe Library:
   - Click Library button
   - Verify: Saved recipe appears in list
4. Load a recipe:
   - Click Load on a recipe
   - Verify: Recipe loads, Info message appears
5. Delete a recipe:
   - Open Library > Click Delete on a recipe > Confirm
   - Verify: Info message appears
6. Test scrolling (if many recipes):
   - If you have several recipes saved, verify list scrolls smoothly
   - Table header should stay visible while scrolling
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm test` passes all existing tests
- [ ] Save shows success/error feedback
- [ ] Delete shows success/error feedback
- [ ] Load shows success feedback (already existed)
- [ ] Recipe list scrolls for large libraries
- [ ] No console errors during normal operation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Storage operations provide clear user feedback
- Library handles 40-50+ recipes smoothly
- Phase 15 complete - v1.2 Recipe Library milestone ready for release
</success_criteria>

<output>
After completion, create `.planning/phases/15-polish/15-01-SUMMARY.md` following summary template.

Update STATE.md:
- Phase: 15 of 15 complete
- Status: v1.2 Recipe Library milestone complete

Update ROADMAP.md:
- Mark Phase 15 complete with date
</output>
