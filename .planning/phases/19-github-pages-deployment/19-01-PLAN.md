---
phase: 19-github-pages-deployment
plan: 01
type: execute
depends_on: []
files_modified: [favicon_io/site.webmanifest]
---

<objective>
Deploy Ice Ed to GitHub Pages, ensuring all paths and ES modules work in production.

Purpose: Make the app accessible from any device via public URL, enabling regular usage without local server.
Output: Working deployment at https://jabbermarky.github.io/icecream/
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech stack:**
- Vanilla JavaScript with ES6 modules (no bundler)
- Static files served directly (index.html, css/, js/, data/)
- External dependency: `https://esm.sh/idb@8` for IndexedDB wrapper

**Key considerations:**
- GitHub Pages serves from subdirectory `/icecream/` (not root)
- All paths must work with subdirectory deployment
- ES modules need correct MIME type (GitHub Pages handles `.js` correctly)
- CDN import for idb library should work unchanged

**From codebase analysis:**
- No build step needed - vanilla JS with ES modules
- All imports use relative paths (`./` or `../`) - these are fine
- Only issue: `site.webmanifest` has absolute paths that need fixing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix webmanifest paths for subdirectory deployment</name>
  <files>favicon_io/site.webmanifest</files>
  <action>Update icon paths from absolute (`/favicon_io/...`) to relative (`android-chrome-192x192.png`). Since webmanifest is in favicon_io/, icons are in same directory, so use just the filename. This ensures paths work regardless of deployment subdirectory.</action>
  <verify>JSON is valid: `python3 -c "import json; json.load(open('favicon_io/site.webmanifest'))"`</verify>
  <done>Webmanifest has relative paths that work from any deployment location</done>
</task>

<task type="auto">
  <name>Task 2: Enable GitHub Pages via gh CLI</name>
  <files>N/A (GitHub settings)</files>
  <action>Use `gh api` to enable GitHub Pages for the repository. Deploy from main branch, root directory. Command: `gh api repos/jabbermarky/icecream/pages -X POST -f source='{"branch":"main","path":"/"}'`. If pages already exist, update with PUT instead of POST.</action>
  <verify>`gh api repos/jabbermarky/icecream/pages` returns page configuration with html_url</verify>
  <done>GitHub Pages enabled, deployment initiated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Deployed Ice Ed to GitHub Pages at https://jabbermarky.github.io/icecream/</what-built>
  <how-to-verify>
    1. Wait 1-2 minutes for GitHub Pages to build and deploy
    2. Visit: https://jabbermarky.github.io/icecream/
    3. Verify: App loads without console errors
    4. Test: Create a new recipe, add an ingredient, verify calculations appear
    5. Test: Save recipe to library, reload page, load recipe from library
    6. Check: Favicon appears in browser tab
    7. Check: No CORS or module loading errors in browser console
  </how-to-verify>
  <resume-signal>Type "approved" if app works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `site.webmanifest` has relative paths
- [ ] GitHub Pages is enabled for the repo
- [ ] App loads at https://jabbermarky.github.io/icecream/
- [ ] All tabs work (Recipe, Ingredients List, Tools, Links, Info & FAQ)
- [ ] Recipe save/load to library works (IndexedDB + esm.sh idb)
- [ ] No console errors related to module loading or CORS
</verification>

<success_criteria>
- Ice Ed accessible at public GitHub Pages URL
- All features work identically to local development
- IndexedDB storage functional in deployed environment
- Phase 19 complete, ready for Phase 20 (Google Drive cloud sync)
</success_criteria>

<output>
After completion, create `.planning/phases/19-github-pages-deployment/19-01-SUMMARY.md`:

# Phase 19 Plan 01: GitHub Pages Deployment Summary

**[One-liner describing deployment outcome]**

## Accomplishments

- [Deployment URL]
- [Any fixes applied]

## Files Created/Modified

- `favicon_io/site.webmanifest` - Fixed paths for subdirectory deployment

## Decisions Made

[Any decisions about deployment configuration]

## Issues Encountered

[Any deployment issues and how they were resolved]

## Next Phase Readiness

Phase 20: Google Drive cloud sync - Now have deployed app URL for OAuth redirect configuration
</output>
