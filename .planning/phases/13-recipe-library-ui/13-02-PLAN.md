---
phase: 13-recipe-library-ui
plan: 02
type: execute
depends_on: ["13-01"]
files_modified: [js/ui/recipe-library.js, js/app.js, test-app.js]
---

<objective>
Wire up load and delete functionality with confirmation and add tests.

Purpose: Enable users to load recipes from the library and delete unwanted recipes with confirmation.
Output: Fully functional library UI with load, delete, and confirmation workflows, plus Playwright tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-recipe-library-ui/13-01-SUMMARY.md

**Key files:**
@js/ui/recipe-library.js
@js/storage/storage.js
@js/app.js
@test-app.js

**Constraining decisions:**
- Phase 12: Storage returns { name, updatedAt, data: { Recipe, Ingredients } }
- Phase 12: deleteRecipe(name) returns Promise<void>
- Must hide modal after successful load
- Must confirm before delete (use confirm() or custom modal)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement load and delete with confirmation</name>
  <files>js/ui/recipe-library.js, js/app.js</files>
  <action>
1. In js/ui/recipe-library.js, enhance Load button handler:
   - Call callbacks.onLoad(recipeName)
   - After onLoad, close the library modal using hideModal()

2. In js/ui/recipe-library.js, enhance Delete button handler:
   - Show confirmation: `confirm(\`Delete "${name}"? This cannot be undone.\`)`
   - If confirmed, call callbacks.onDelete(recipeName)
   - After delete, refresh the list by re-calling storage.listRecipes() and re-rendering
   - Alternative: close modal after delete (simpler, user re-opens if needed)

3. In js/app.js, complete the onDelete callback:
   ```javascript
   onDelete: async (name) => {
       await recipeStorage.deleteRecipe(name);
       Info(`Deleted "${name}" from library`);
   }
   ```

4. Handle edge cases:
   - If loadRecipe returns null, show warning: `Warning(\`Recipe "${name}" not found\`)`
   - If listRecipes returns empty after delete, show empty state

5. After successful load, update Recipe UI:
   - Existing setRecipe() handles Recipe and Ingredients
   - Mark recipe as unmodified (if applicable)
  </action>
  <verify>
Manual test: Save a recipe (Phase 14 handles save, so test with test method if available).
If no save yet, manually add test data via console:
`getRecipeStorage().saveRecipe({name: 'Test Recipe', data: {Recipe: window.Recipe, Ingredients: {...}}})`
Then open Library, verify Load and Delete work.
  </verify>
  <done>Load retrieves and displays recipe, Delete removes with confirmation, modal closes appropriately</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright tests for recipe library</name>
  <files>test-app.js</files>
  <action>
Add new test method `testRecipeLibrary()` that:

1. Pre-populate storage with test recipe:
   - Use testRecipeStorage() pattern from Phase 12 or call storage directly
   - Save a recipe with known name "Library Test Recipe"

2. Test library modal opens:
   - Click btnRecipeLibrary
   - Assert modal is visible
   - Assert recipe name appears in list

3. Test load functionality:
   - Click Load button for "Library Test Recipe"
   - Assert modal closes
   - Assert recipe name input (edRecipeName) contains "Library Test Recipe"

4. Test delete functionality:
   - Re-open library (save another test recipe first: "Recipe To Delete")
   - Click Delete for "Recipe To Delete"
   - Handle confirm dialog (page.on('dialog'))
   - Assert recipe removed from list OR modal closes

5. Clean up:
   - Delete any test recipes from storage after test

Add to test runner if tests are run in sequence.
  </action>
  <verify>`npm test` passes including new testRecipeLibrary test</verify>
  <done>Playwright tests cover library open, load, and delete workflows</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes including new tests
- [ ] Load button loads recipe and closes modal
- [ ] Delete button shows confirmation, deletes on confirm
- [ ] Status bar shows feedback for load/delete actions
- [ ] Empty state shown when all recipes deleted
</verification>

<success_criteria>

- Load functionality works end-to-end
- Delete functionality includes confirmation
- Tests pass for library workflows
- Phase 13 complete
</success_criteria>

<output>
After completion, create `.planning/phases/13-recipe-library-ui/13-02-SUMMARY.md`
</output>
