---
phase: 09-extract-recipe-manager
plan: 01
type: execute
depends_on: []
files_modified: [js/features/recipe-manager.js, js/app.js]
---

<objective>
Create recipe-manager.js module with core recipe state and display functions.

Purpose: Begin the recipe manager extraction by moving recipe state management and display rendering.
Output: New js/features/recipe-manager.js module with core functions, app.js updated with imports.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-extract-models/08-01-SUMMARY.md

**Tech stack available:** ES6 modules, dependency injection pattern
**Established patterns:**
- Dependency injection via init function (from tools.js, models/core.js)
- Constructor defaults pattern (from models/core.js)
- Return handler functions from init for external use (from tools.js)

**Constraining decisions:**
- Phase 8: Constructor defaults object for cRecipe - decouple class from DOM
- Phase 8: Inject RecipeDataColumns via getRecipeDataColumns
- Phase 7: Deferred Recipe access via getRecipe function

**Key files:**
@js/app.js
@js/models/core.js
@js/features/ingredients.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recipe-manager.js with core state functions</name>
  <files>js/features/recipe-manager.js</files>
  <action>
Create new module js/features/recipe-manager.js with:

1. Imports needed from other modules:
   - toFloat, round, nGenerator from helpers.js
   - filterPosNumberInput from helpers.js
   - cRecipe from models/core.js
   - Info, Warning, ErrorMsg from ui/components.js

2. Module-level state (will be initialized via initRecipeManager):
   - RecipeBackup array
   - RecipeStack object
   - sortBy, sortAsc variables
   - Recipe reference (via getRecipe injection)
   - Dependency references (getRecipeDataColumns, etc.)

3. Extract these functions from app.js (lines 201-456):
   - SetRecipeModified() - track modified state
   - IsRecipeModified() - check modified state
   - BackupCurrentRecipe() - add current recipe to backup
   - BackupRecipe() - backup a specific recipe
   - DisplayBackupList() - render backup list in DOM
   - RestoreBackup() - restore a backed-up recipe
   - SortRecipe() - sort recipe ingredients
   - CreateRecipeRow() - create DOM row for recipe ingredient
   - DisplayRecipe() - render full recipe to DOM
   - UpdateRecipeRow() - update single row values

4. Create initRecipeManager() function that:
   - Accepts dependencies: { getRecipe, setRecipe, getIngredients, getRecipeDataColumns, getRecipeColumns, sliders (slServingTemperature, slHardness, slOverrun, slScoopSize), scoopSizes, tgtSelection, updateRecipeSums, showModal, hideModal, Info, Warning, ErrorMsg }
   - Stores these for use by other functions
   - Returns exposed functions for app.js to use

5. Export: initRecipeManager and all extracted functions

Note: Recipe is mutable state shared with app.js. Use getRecipe/setRecipe for Recipe access. RecipeBackup and RecipeStack are owned by this module.
  </action>
  <verify>File exists, has proper ES6 exports, no syntax errors: node --check js/features/recipe-manager.js</verify>
  <done>recipe-manager.js created with ~300 lines of core state/display functions</done>
</task>

<task type="auto">
  <name>Task 2: Update app.js to use recipe-manager core functions</name>
  <files>js/app.js</files>
  <action>
1. Add import statement for recipe-manager.js:
   import { initRecipeManager, SetRecipeModified, IsRecipeModified, DisplayRecipe, CreateRecipeRow, UpdateRecipeRow, DisplayBackupList, SortRecipe } from './features/recipe-manager.js';

2. Remove extracted function definitions from app.js:
   - SetRecipeModified (lines 201-207)
   - IsRecipeModified (lines 209-211)
   - BackupCurrentRecipe (lines 213-216)
   - BackupRecipe (lines 217-235)
   - DisplayBackupList (lines 236-262)
   - RestoreBackup (lines 263-272)
   - SortRecipe (lines 274-307)
   - CreateRecipeRow (lines 309-374)
   - DisplayRecipe (lines 376-435)
   - UpdateRecipeRow (lines 437-456)

3. Remove module-level state that moves to recipe-manager:
   - RecipeBackup (line 94)
   - RecipeStack (line 95)
   - sortBy, sortAsc (lines 96-97)

4. Initialize recipe-manager after Recipe is defined (around line 182):
   initRecipeManager({
     getRecipe: () => Recipe,
     setRecipe: (r) => { Recipe = r; },
     getIngredients: () => Ingredients,
     getRecipeDataColumns: () => RecipeDataColumns,
     getRecipeColumns: () => RecipeColumns,
     sliders: { slServingTemperature, slHardness, slOverrun, slScoopSize },
     scoopSizes,
     tgtSelection,
     updateRecipeSums: () => UpdateRecipeSums(),
     showModal,
     hideModal,
     Info,
     Warning,
     ErrorMsg
   });

5. Ensure all calls to extracted functions still work (DisplayRecipe, SetRecipeModified, etc. are now imports)
  </action>
  <verify>npm test passes all tests</verify>
  <done>app.js imports recipe-manager, tests pass, no runtime errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check js/features/recipe-manager.js` passes
- [ ] `npm test` passes all tests
- [ ] Recipe tab displays correctly (visual check if needed)
- [ ] Recipe backup/restore works
</verification>

<success_criteria>

- js/features/recipe-manager.js created (~300 lines)
- app.js reduced by ~250 lines
- All existing tests pass
- Recipe display and backup functionality works
</success_criteria>

<output>
After completion, create `.planning/phases/09-extract-recipe-manager/09-01-SUMMARY.md`
</output>
