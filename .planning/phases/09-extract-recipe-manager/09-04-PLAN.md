---
phase: 09-extract-recipe-manager
plan: 04
type: execute
depends_on: ["09-03"]
files_modified: [js/app.js, index.html]
---

<objective>
Final cleanup: remove Check for Updates feature and verify final app.js size.

Purpose: Address ISS-001 (remove deprecated feature) and complete Phase 9.
Output: Clean app.js at ~150 lines, Phase 9 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/ISSUES.md
@.planning/phases/09-extract-recipe-manager/09-03-SUMMARY.md

**Issues being addressed:**
- ISS-001: Remove Check for Updates functionality
- ISS-002: httpRequest undeclared variable (resolved via ISS-001)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Check for Updates feature from app.js and HTML</name>
  <files>js/app.js, index.html</files>
  <action>
1. In js/app.js, remove the Check for Updates handler (lines ~1146-1206):
   - Remove entire document.getElementById("btnCheckUpdate").onclick handler
   - Remove the cVersion class definition inside it
   - This removes the undeclared httpRequest variables (ISS-002)

2. In index.html, remove or disable the Check for Updates button:
   - Find the btnCheckUpdate button element
   - Remove it or comment it out
   - Remove the VersionInfo span if present

3. Keep the VERSION constant and Version display in About section

4. Verify no references to btnCheckUpdate or httpRequest remain
  </action>
  <verify>grep -n "btnCheckUpdate\|httpRequest" js/app.js index.html returns no results</verify>
  <done>Check for Updates feature removed, ISS-001 and ISS-002 resolved</done>
</task>

<task type="auto">
  <name>Task 2: Verify final app.js structure and line count</name>
  <files>js/app.js</files>
  <action>
1. Review app.js to ensure it contains only:
   - Import statements (~15 lines)
   - VERSION constant
   - RecipeDataColumns, RecipeColumns constants
   - Module initialization calls (initModels, initUIComponents, initTabs, initTools, initIngredients, initRecipeManager, initRecipeButtons)
   - Slider/DOM setup (slServingTemperature, slHardness, slOverrun, slScoopSize, scoopSizes)
   - Target selection dropdown setup
   - Recipe initialization
   - Tab click initialization
   - Links section building
   - About section TOC
   - replaceAll polyfill
   - window.Recipe exposure for testing

2. Count lines: wc -l js/app.js
   - Target: ~150-200 lines
   - If significantly over, identify what else can be extracted

3. If cleanup needed, consolidate or remove dead code

4. Run full test suite to confirm everything works
  </action>
  <verify>npm test passes all tests AND wc -l js/app.js shows ~150-200 lines</verify>
  <done>app.js at target size, all tests pass, Phase 9 complete</done>
</task>

<task type="auto">
  <name>Task 3: Update ISSUES.md to close resolved issues</name>
  <files>.planning/ISSUES.md</files>
  <action>
1. Move ISS-001 and ISS-002 from "Open Enhancements" to "Closed Enhancements"
2. Add resolution date and how they were resolved
3. Note that they were resolved in Phase 9 Plan 04
  </action>
  <verify>cat .planning/ISSUES.md shows issues moved to Closed section</verify>
  <done>Issues log updated with closed items</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes all tests
- [ ] `wc -l js/app.js` shows ~150-200 lines
- [ ] Check for Updates button removed from HTML
- [ ] No httpRequest or btnCheckUpdate references remain
- [ ] ISSUES.md updated
- [ ] All recipe functionality works end-to-end
</verification>

<success_criteria>

- app.js at ~150-200 lines (orchestration only)
- ISS-001 and ISS-002 resolved
- All tests pass
- Phase 9 complete, modularization finished
</success_criteria>

<output>
After completion, create `.planning/phases/09-extract-recipe-manager/09-04-SUMMARY.md` with:
- Final line counts for all modules
- List of all modules created during modularization
- Confirmation that project target achieved (app.js from 1,666 to ~150 lines)
</output>
