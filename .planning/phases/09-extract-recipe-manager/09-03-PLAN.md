---
phase: 09-extract-recipe-manager
plan: 03
type: execute
depends_on: ["09-02"]
files_modified: [js/features/recipe-manager.js, js/app.js]
---

<objective>
Add optimization and button handlers to recipe-manager.js.

Purpose: Complete the recipe manager extraction with optimization algorithms and all button handlers.
Output: recipe-manager.js with full recipe operations (~900 lines).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-extract-recipe-manager/09-02-SUMMARY.md

**Key files:**
@js/features/recipe-manager.js
@js/app.js
@js/utils/file-io.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optimization and CRUD operations to recipe-manager.js</name>
  <files>js/features/recipe-manager.js</files>
  <action>
Add imports needed:
- saveToFile, parseRecipeFile from file-io.js
- cIngredient, importIngredients from ingredients.js

Extract and add these functions from app.js:

1. Optimization functions (lines 721-868):
   - OptimizeRecipe() - hill-climbing optimization algorithm
   - RestoreRecipe() - restore from optimization backup
   - CategorizeRecipe() - auto-detect recipe type based on fitness

2. Button handler implementations (lines 870-1011):
   - handleNewRecipe() - create new recipe (from btnNewRecipe.onclick)
   - handleStoreAsIngredient() - store recipe as ingredient
   - handleSaveRecipe() - save recipe to file
   - handleLoadRecipe() - load recipe from file (the reader.onload logic)

3. Create setup function to attach button handlers:
   - initRecipeButtons() - attaches onclick handlers to recipe buttons
   - Takes button element references as dependencies
   - Keeps handler implementations in this module

Update initRecipeManager to accept:
- saveToFile, parseRecipeFile (from file-io.js)
- cIngredient, importIngredients (from ingredients.js)

Export: OptimizeRecipe, RestoreRecipe, CategorizeRecipe, initRecipeButtons
  </action>
  <verify>node --check js/features/recipe-manager.js passes</verify>
  <done>Optimization and CRUD handlers added to recipe-manager.js (~900 total lines)</done>
</task>

<task type="auto">
  <name>Task 2: Update app.js to use recipe-manager operations</name>
  <files>js/app.js</files>
  <action>
1. Update import from recipe-manager.js to include:
   - OptimizeRecipe, RestoreRecipe, CategorizeRecipe
   - initRecipeButtons

2. Remove extracted functions from app.js:
   - OptimizeRecipe (lines 721-843)
   - RestoreRecipe (lines 845-851)
   - CategorizeRecipe (lines 853-868)
   - Button handler inline functions (lines 870-1027)

3. Replace button handler setup with initRecipeButtons call:
   initRecipeButtons({
     btnNewRecipe: document.getElementById('btnNewRecipe'),
     btnStoreAsIngredient: document.getElementById('btnStoreAsIngredient'),
     btnSaveRecipe: document.getElementById('btnSaveRecipe'),
     btnLoadRecipe: document.getElementById('btnLoadRecipe'),
     inputLoadRecipe: document.getElementById('inputLoadRecipe'),
     btnPrintRecipe: document.getElementById('btnPrintRecipe'),
     btnCategorizeRecipe: document.getElementById('btnCategorizeRecipe'),
     btnOptimizeMean: document.getElementById('btnOptimizeMean'),
     btnOptimizeRange: document.getElementById('btnOptimizeRange'),
     btnRestoreRecipe: document.getElementById('btnRestoreRecipe'),
     btnScale: document.getElementById('btnScale'),
     cbxScaleByIngredient: document.getElementById('cbxScaleByIngredient'),
     edTargetWeight: document.getElementById('edTargetWeight'),
     selTargetWeightMode: document.getElementById('selTargetWeightMode'),
     edRecipeName: document.getElementById('edRecipeName')
   });

4. Update initRecipeManager call to pass additional dependencies:
   - saveToFile, parseRecipeFile
   - cIngredient, importIngredients
   - clickOn (from helpers.js)
  </action>
  <verify>npm test passes all tests</verify>
  <done>app.js reduced by ~350 additional lines, all recipe operations work</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check js/features/recipe-manager.js` passes
- [ ] `npm test` passes all tests
- [ ] Recipe optimization works
- [ ] Save/load recipe works
- [ ] New recipe button works
- [ ] Store as ingredient works
</verification>

<success_criteria>

- recipe-manager.js at ~900 lines
- app.js reduced by ~350 additional lines
- All tests pass
- Full recipe CRUD and optimization works
</success_criteria>

<output>
After completion, create `.planning/phases/09-extract-recipe-manager/09-03-SUMMARY.md`
</output>
