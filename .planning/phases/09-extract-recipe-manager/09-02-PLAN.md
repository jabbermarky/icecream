---
phase: 09-extract-recipe-manager
plan: 02
type: execute
depends_on: ["09-01"]
files_modified: [js/features/recipe-manager.js, js/app.js]
---

<objective>
Add recipe event handlers and sums calculation to recipe-manager.js.

Purpose: Continue extraction by moving event handlers and the core UpdateRecipeSums function.
Output: recipe-manager.js expanded with event handlers and sums calculation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-extract-recipe-manager/09-01-SUMMARY.md

**Key files:**
@js/features/recipe-manager.js
@js/app.js
@js/features/calculations.js
@js/ui/graph.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add event handlers to recipe-manager.js</name>
  <files>js/features/recipe-manager.js</files>
  <action>
Add imports needed:
- GetIdealPAC, Fitness from calculations.js
- DrawFreezingGraph from ui/graph.js
- cTargetValue, Targets from models/core.js
- IngredientDataFields from ingredients.js
- getCSS from ui/components.js

Extract and add these functions from app.js:

1. Event handlers (lines 458-564):
   - onIngredientChanged() - handle ingredient dropdown change
   - onIngredientAmountEdited() - handle amount input change
   - onRecipeIngredientDeleted() - handle delete button click
   - onRecipeScaled() - handle scale button click
   - onScaleInputKeyUp() - handle enter key in scale inputs
   - ToggleIngredientScale() - handle scale mode checkbox

2. Recipe sums and info (lines 566-708):
   - UpdateRecipeSums() - recalculate and display recipe totals
   - Normalize() - helper for normalized values
   - UpdateRecipeInfo() - update recipe info panel
   - CheckRecipe() - validate recipe and show hints

3. Helper functions from end of app.js:
   - gToL() - grams to liters conversion (line 1222)
   - LToG() - liters to grams conversion (line 1223)

Update initRecipeManager to accept additional dependencies:
- GetIdealPAC, Fitness (from calculations.js)
- DrawFreezingGraph (from ui/graph.js)
- getCSS (from ui/components.js)
- IngredientDataFields (from ingredients.js)
- cTargetValue, Targets (from models/core.js)

Export new functions: UpdateRecipeSums, UpdateRecipeInfo, onIngredientChanged, onIngredientAmountEdited, onRecipeIngredientDeleted, onRecipeScaled, onScaleInputKeyUp, ToggleIngredientScale, gToL, LToG
  </action>
  <verify>node --check js/features/recipe-manager.js passes</verify>
  <done>Event handlers and sums functions added to recipe-manager.js (~450 total lines)</done>
</task>

<task type="auto">
  <name>Task 2: Update app.js to use recipe-manager event handlers</name>
  <files>js/app.js</files>
  <action>
1. Update import from recipe-manager.js to include:
   - UpdateRecipeSums, UpdateRecipeInfo
   - onIngredientChanged, onIngredientAmountEdited, onRecipeIngredientDeleted
   - onRecipeScaled, onScaleInputKeyUp, ToggleIngredientScale
   - gToL, LToG

2. Remove extracted functions from app.js:
   - onIngredientChanged (lines 458-478)
   - onIngredientAmountEdited (lines 480-496)
   - onRecipeIngredientDeleted (lines 498-509)
   - onRecipeScaled (lines 511-556)
   - onScaleInputKeyUp (lines 558-564)
   - UpdateRecipeSums (lines 566-628)
   - Normalize (lines 630-633)
   - UpdateRecipeInfo (lines 635-668)
   - CheckRecipe (lines 671-708)
   - ToggleIngredientScale (lines 710-717)
   - gToL, LToG (lines 1222-1223)

3. Update initRecipeManager call to pass additional dependencies:
   - GetIdealPAC, Fitness
   - DrawFreezingGraph
   - getCSS
   - IngredientDataFields
   - cTargetValue, Targets

4. Ensure slider handlers still call UpdateRecipeSums correctly:
   - slServingTemperature.oninput calls UpdateRecipeSums
   - slHardness.oninput calls UpdateRecipeSums
   - tgtSelection.onchange calls UpdateRecipeSums

Note: UpdateRecipeSums is now imported, used directly (no wrapper needed)
  </action>
  <verify>npm test passes all tests</verify>
  <done>app.js reduced by ~300 lines, all recipe functionality works</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check js/features/recipe-manager.js` passes
- [ ] `npm test` passes all tests
- [ ] Recipe calculations update correctly when changing amounts
- [ ] Scaling recipes works
- [ ] Recipe hints display correctly
</verification>

<success_criteria>

- recipe-manager.js expanded to ~600 lines
- app.js reduced by ~300 additional lines
- All tests pass
- Recipe editing and calculation works
</success_criteria>

<output>
After completion, create `.planning/phases/09-extract-recipe-manager/09-02-SUMMARY.md`
</output>
