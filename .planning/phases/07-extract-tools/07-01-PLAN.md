---
phase: 07-extract-tools
plan: 01
type: execute
depends_on: []
files_modified: [js/utils/tools.js, js/app.js]
---

<objective>
Extract calculator tools from `js/app.js` to `js/utils/tools.js`.

Purpose: Continue modularization to improve maintainability and reduce app.js complexity.
Output: New `js/utils/tools.js` module (~314 lines) with PAC/POD, G/Mol, and Egg/Yolk calculators.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./07-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md

**Source code to extract (js/app.js lines 1159-1472):**
- `Sugars` constant - Sugar reference table with g/mol and POD values
- `onSugarAmountEdited()` - Handler for PAC/POD calculator inputs
- `UpdateSugarSums()` - Recalculate PAC/POD sums
- PAC/POD table initialization block (pacPodCalc, pacPodTable)
- G/Mol calculator initialization (edGMolCalculator)
- `eggTypes` constant - Egg weight standards by region
- `cEgg` class - Egg calculator model
- Yolk calculator initialization (yolkCalc, yolkTable)
- `InitYolkTable()` - Initialize yolk table values
- `EggValueChanged()` - Handler for yolk inputs
- `UpdateEggYolkValues()` - Update all egg values from model

**Established extraction pattern (from ui/components.js):**
- Use dependency injection for cross-module dependencies
- Export `init*()` function that receives dependencies
- Named exports only, no default exports
- JSDoc comments for public functions

**Dependencies needed:**
- From helpers.js: `toFloat`, `round`, `nGenerator`, `filterPosNumberInput`
- From ui/components.js: `Warning`, `Info`
- From app.js: `Recipe` object (inject via init function)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create js/utils/tools.js module</name>
  <files>js/utils/tools.js</files>
  <action>
Create new module with the following structure:

1. **Header and imports:**
   ```javascript
   /**
    * Tools Module
    * PAC/POD Calculator, G/Mol Calculator, Egg/Yolk Calculator
    */
   import { toFloat, round, nGenerator, filterPosNumberInput } from './helpers.js';
   import { Warning, Info } from '../ui/components.js';
   ```

2. **Dependency injection:**
   ```javascript
   let getRecipe = null;  // Function to get current Recipe object

   export function initTools(deps) {
       if (deps.getRecipe) getRecipe = deps.getRecipe;
   }
   ```

3. **Extract and export:**
   - `Sugars` constant (export const)
   - `eggTypes` constant (export const)
   - `cEgg` class (export class)
   - `initPACPODCalculator()` - wrap PAC/POD table setup in function
   - `initGMolCalculator()` - wrap G/Mol setup in function
   - `initYolkCalculator()` - wrap yolk table setup in function, returns `InitYolkTable` for use by app.js

4. **Key modifications during extraction:**
   - Change `Recipe.Sums.Amount` references to `getRecipe().Sums.Amount`
   - Change `Recipe.Amount = value` to `getRecipe().Amount = value`
   - Keep internal functions (`onSugarAmountEdited`, `UpdateSugarSums`, `EggValueChanged`, `UpdateEggYolkValues`) as module-level but not exported

5. **Export InitYolkTable:** The yolk calculator's `InitYolkTable()` is called from app.js tab handler. Return it from `initYolkCalculator()`:
   ```javascript
   export function initYolkCalculator() {
       // ... setup code ...
       return { InitYolkTable };
   }
   ```
  </action>
  <verify>File exists and has valid JavaScript syntax: `node --check js/utils/tools.js`</verify>
  <done>js/utils/tools.js created with all calculator code, ~314 lines, valid ES6 module</done>
</task>

<task type="auto">
  <name>Task 2: Update js/app.js to import and use tools module</name>
  <files>js/app.js</files>
  <action>
1. **Add import statement** (after existing imports, around line 35):
   ```javascript
   import { initTools, initPACPODCalculator, initGMolCalculator, initYolkCalculator, Sugars, eggTypes, cEgg } from './utils/tools.js';
   ```

2. **Initialize tools with Recipe dependency** (after Recipe is defined, likely after the IIFE starts):
   - Find where Recipe object is created/available
   - Add: `initTools({ getRecipe: () => Recipe });`

3. **Call initialization functions** (replace the inline tool setup blocks):
   - Replace PAC/POD calculator block (lines ~1222-1282) with: `initPACPODCalculator();`
   - Replace G/Mol calculator block (lines ~1285-1302) with: `initGMolCalculator();`
   - Replace yolk calculator block (lines ~1306-1471) with: `const { InitYolkTable } = initYolkCalculator();`

4. **Remove extracted code:**
   - Delete `Sugars` constant (lines 1160-1177)
   - Delete `onSugarAmountEdited()` function (lines 1178-1198)
   - Delete `UpdateSugarSums()` function (lines 1200-1220)
   - Delete PAC/POD table setup block (lines 1222-1282)
   - Delete G/Mol calculator setup (lines 1285-1302)
   - Delete `eggTypes` object (lines 1306-1337)
   - Delete `cEgg` class (lines 1339-1372)
   - Delete yolk calculator setup block (lines 1375-1471)

5. **Keep InitYolkTable reference** for tab handler:
   - The tab switch handler at line 64 calls `InitYolkTable()` - this should still work after extraction
   - Ensure `InitYolkTable` is accessible in the tab handler scope
  </action>
  <verify>File has valid syntax: `node --check js/app.js` (note: will show import errors in Node but syntax should be valid)</verify>
  <done>app.js imports tools module, calls init functions, extracted code removed (~314 lines removed)</done>
</task>

<task type="auto">
  <name>Task 3: Run test suite and verify functionality</name>
  <files>test-app.js</files>
  <action>
Run the full test suite to verify no regressions:

```bash
npm test
```

If tests fail:
1. Check console errors for import/export issues
2. Verify Recipe dependency is properly injected
3. Ensure InitYolkTable is accessible from tab handler
4. Fix any scope or reference issues

The test suite includes:
- `testPageLoads` - Verify page loads without errors
- `testTabNavigation` - Verify Tools tab is accessible
- All other existing tests should pass unchanged
  </action>
  <verify>`npm test` exits with code 0, all tests pass</verify>
  <done>All tests pass, no console errors, Tools tab functions correctly</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `node --check js/utils/tools.js` passes (valid syntax)
- [ ] `npm test` passes all tests
- [ ] No console errors when loading page
- [ ] PAC/POD Calculator works (enter values, see calculations)
- [ ] G/Mol Calculator works (enter g/mol, see PAC result)
- [ ] Egg/Yolk Calculator works (select egg type, enter values, use From/To Recipe buttons)
- [ ] Tools tab accessible and functional
</verification>

<success_criteria>

- js/utils/tools.js created with ~314 lines
- js/app.js reduced by ~314 lines (from 1,666 to ~1,352)
- All existing tests pass
- All three calculators function correctly
- No console errors in browser
</success_criteria>

<output>
After completion, create `.planning/phases/07-extract-tools/07-01-SUMMARY.md`:

```markdown
# Phase 7 Plan 01: Extract Tools Module Summary

**[One-liner describing what was accomplished]**

## Accomplishments

- Created js/utils/tools.js with calculator tools
- Reduced js/app.js by ~314 lines
- [Other accomplishments]

## Files Created/Modified

- `js/utils/tools.js` - New module with PAC/POD, G/Mol, Egg/Yolk calculators
- `js/app.js` - Removed tool code, added imports and init calls

## Decisions Made

[Any decisions and rationale]

## Issues Encountered

[Problems and resolutions]

## Next Step

Ready for Phase 8: Extract Core Models
```
</output>
